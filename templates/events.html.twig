{% extends 'base.html.twig' %}

{% block title %}Evenements{% endblock %}

{% block titre %}Evenements{% endblock %}

{% block body %}
<div class="row">
    <div class="col">
        <a class="btn btn-primary" href="{{ path('event_add') }}" role="button">ajout</a>
    </div>
</div>
<div class="row">
    <div class="col">
        {# {{ sg_datatables_render(produitsDatatable) }} #}
    </div>
</div>
{% endblock %}
